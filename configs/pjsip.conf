; =============================================================================
; pjsip.conf — Asterisk PJSIP — Opérateur 1
;
; Linphone depuis l'hôte :
;   Direct : 172.20.0.11:5060  (Docker bridge)
;   Mappé  : 10.2.0.2:5060
;   linphone_A / tester → 100  |  linphone_B / testerB → 200
; =============================================================================

[global]
; [2026-08-09] Trace SIP persistante. `pjsip set logger on` est un reglage
; d execution qui ne survit pas au redemarrage d Asterisk (mesure : capture armee
; a 02:52, perdue au redemarrage de 07:32, appel Linphone reussi NON capture).
; A remettre a no une fois la comparaison INVITE-qui-marche / INVITE-qui-echoue
; faite : cette option journalise TOUS les messages SIP, y compris les REGISTER.
debug=yes
type=global
user_agent=OsmoAsterisk-Op1
; Les clients (Linphone) et le trunk WAN arrivent tous via la gateway Docker
; 172.20.0.1 (NAT). L'identify IP du trunk WAN (network/setup-wan-interop.sh, match=
; 172.20.0.1) avalerait sinon les REGISTER Linphone. On identifie donc par
; username d'abord : Linphone -> par username, trunk WAN IP-only -> par IP.
endpoint_identifier_order=username,ip,anonymous

; =============================================================================
; TRANSPORT
; =============================================================================

[transport-udp]
type=transport
protocol=udp
bind=0.0.0.0
external_media_address=10.2.0.2
external_signaling_address=10.2.0.2
local_net=172.20.0.0/16
local_net=127.0.0.0/8

; =============================================================================
; LINPHONE A — numéro 100
; =============================================================================

[linphone_A]
type=endpoint
transport=transport-udp
context=internal
disallow=all
allow=alaw
allow=ulaw
allow=gsm
auth=linphone_A
aors=linphone_A
direct_media=no
rtp_symmetric=yes
force_rport=yes
rewrite_contact=yes
media_encryption=no
callerid=Linphone A <100>

[linphone_A]
type=auth
auth_type=userpass
username=linphone_A
password=tester

[linphone_A]
type=aor
max_contacts=3
remove_existing=yes
qualify_frequency=30
qualify_timeout=5.0

; =============================================================================
; LINPHONE B — numéro 200
; =============================================================================

[linphone_B]
type=endpoint
transport=transport-udp
context=internal
disallow=all
allow=alaw
allow=ulaw
allow=gsm
auth=linphone_B
aors=linphone_B
direct_media=no
rtp_symmetric=yes
force_rport=yes
rewrite_contact=yes
media_encryption=no
callerid=Linphone B <200>

[linphone_B]
type=auth
auth_type=userpass
username=linphone_B
password=testerB

[linphone_B]
type=aor
max_contacts=3
remove_existing=yes
qualify_frequency=30
qualify_timeout=5.0

; =============================================================================
; TRUNK GSM ← OsmoMSC via osmo-sip-connector (127.0.0.1:5061)
;
; media_encryption=no : CRITIQUE — osmo-sip-connector envoie du RTP brut,
;   pas du SRTP/DTLS. Sans cette option Asterisk peut interpréter certains
;   paquets RTP comme du DTLS → "Received SSL traffic without SSL session"
;   → audio jeté → pas de son.
; =============================================================================

[gsm_msc-identify]
type=identify
endpoint=gsm_msc
match=127.0.0.1

[gsm_msc]
type=endpoint
transport=transport-udp
context=gsm_in
disallow=all
allow=gsm
allow=ulaw
aors=gsm_msc
direct_media=no
rtp_symmetric=yes
force_rport=yes
rewrite_contact=yes
media_encryption=no
dtls_auto_generate_cert=no

[gsm_msc]
type=aor
contact=sip:127.0.0.1:5061
qualify_frequency=10

; =============================================================================
; TRUNKS INTER-OPÉRATEURS — GÉNÉRÉ par start.sh
; =============================================================================
;
; =============================================================================
; TRUNK ZADARMA (opérateur SIP externe)  — https://zadarma.com
;
; ┌─ À REMPLIR ─────────────────────────────────────────────────────────────┐
; │  ZADARMA_LOGIN_A_REMPLIR         ← votre login SIP Zadarma (ex: 123456)  │
; │  ZADARMA_PASSWORD_A_REMPLIR      ← votre mot de passe SIP              │
; └─────────────────────────────────────────────────────────────────────────┘
; Les deux se trouvent dans l'espace client Zadarma, section « Mes SIP ».
; ⚠️ Le login SIP N'EST PAS l'identifiant du compte web, et le mot de passe SIP
;    n'est pas celui du compte : ce sont des identifiants dédiés.
;
; Après remplissage :  asterisk -rx "pjsip reload"
; Vérifier :           asterisk -rx "pjsip show registrations"   → Registered
;
; Composition :
;   sortant  → préfixe 00 + numéro international   (ex: 0033612345678)
;   entrant  → contexte [zadarma_in] du dialplan
; =============================================================================

[zadarma_auth]
type=auth
auth_type=userpass
username=533110
password=WBetNYls24

[zadarma]
type=aor
contact=sip:sip.zadarma.com
qualify_frequency=60
qualify_timeout=5.0

[zadarma_reg]
type=registration
transport=transport-udp
outbound_auth=zadarma_auth
server_uri=sip:sip.zadarma.com
client_uri=sip:533110@sip.zadarma.com
contact_user=533110
retry_interval=60
forbidden_retry_interval=600
expiration=300
line=yes
endpoint=zadarma

[zadarma]
type=endpoint
transport=transport-udp
context=zadarma_in
disallow=all
allow=alaw
allow=ulaw
allow=gsm
outbound_auth=zadarma_auth
aors=zadarma
from_user=533110
from_domain=sip.zadarma.com
; NAT : Asterisk est derrière la gateway Docker. Sans ces quatre lignes le RTP
; part vers une adresse privée et l'appel est muet dans un sens.
direct_media=no
rtp_symmetric=yes
force_rport=yes
rewrite_contact=yes
; Zadarma ne fait pas de SRTP sur ce type de compte. Laisser 'no' : sinon
; Asterisk peut prendre du RTP brut pour du DTLS et jeter l'audio (même piège
; que le trunk gsm_msc plus haut dans ce fichier).
media_encryption=no
dtmf_mode=rfc4733

[zadarma-identify]
type=identify
endpoint=zadarma
match=sip.zadarma.com
; ⚠️ endpoint_identifier_order (en tête de ce fichier) vaut username,ip,anonymous.
;    L'identification par IP n'intervient donc qu'après le username : les REGISTER
;    des Linphone ne peuvent pas être avalés par ce match.
