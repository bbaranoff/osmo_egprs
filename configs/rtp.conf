; rtp.conf — Asterisk RTP configuration
; Plage de ports pour le média RTP (voice)
;
; En mode Linphone externe, ces ports doivent être exposés
; via Docker (-p) et ouverts dans le firewall.

[general]
rtpstart=__RTP_START__
rtpend=__RTP_END__
strictrtp=no
icesupport=no
stunaddr=
