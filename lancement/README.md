# `lancement/` — Points d'entrée secondaires


Lancements historiques et variantes. Le point d'entrée principal est `../run.sh` ;
`../start-direct.sh` orchestre la pile complète et reste à la racine.

| Fichier | |
|---|---|
| `osmo-launch.sh` | ══════════════════════════════════════════════════════════════════════════════ |
| `start-in-iso.sh` | lancement/start-in-iso.sh — lancement NATIF (hors docker) du lab osmo-nitb-for-calypso sur |
| `start-oqc.sh` | lancement/start-oqc.sh — lance osmo-qemu-calypso depuis osmo-nitb-for-calypso |
| `start.sh` | lancement/start.sh — Lance la stack Osmocom GSM multi-opérateurs |
